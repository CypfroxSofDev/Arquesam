<!DOCTYPE html>
<html>
<head>
  <title>Arquesam Creativo</title>
  <link href="{{ asset('css/style.css') }}" type="text/css" rel="stylesheet" media="all">
  <link href="{{ asset('css/metro.css') }}" rel="stylesheet">
  <link href="{{ asset('css/metro-icons.css') }}" rel="stylesheet">
  <!--js-->
  <script src="{{ asset('js/jquery-2.1.3.min.js') }}"></script>
  <script src="{{ asset('js/metro.js') }}"></script>
  <script>
    $(function(){
        var form = $(".login-form");

        form.css({
            opacity: 1,
            "-webkit-transform": "scale(1)",
            "transform": "scale(1)",
            "-webkit-transition": ".5s",
            "transition": ".5s"
        });
    });
  </script>
  
</head>
<body class="bg-darkTeal">      
  <div class="login-form padding20 block-shadow">
    {% if error %}
    <script>
        $.Notify({keepOpen: true, type: 'alert', caption: '- Problemas en el acceso:', content: "<br>El <strong>usuario</strong> o <strong>contraseña</strong> son incorrecto, verifique sus datos e intente nuevamente."});
    </script>
    {% endif %}  
    <form action="login_check" method="post">
        <h1 class="text-light">Bienvenido</h1>
        <hr class="thin"/>
        <br />
        <div class="input-control text full-size" data-role="input">
            <label for="user_login">Username:</label>
            <input type="text" name="_username" id="user_login" value="{{ last_username }}">
            <button class="button helper-button clear"><span class="mif-cross"></span></button>
        </div>
        <br />
        <br />
        <div class="input-control password full-size" data-role="input">
            <label for="user_password">User password:</label>
            <input type="password" name="_password" id="user_password">
            <button class="button helper-button reveal"><span class="mif-looks"></span></button>
        </div>
        <br />
        <br />
        <div class="form-actions">
            <button type="submit" class="button primary">Entrar...</button>
            <button type="button" class="button link"><a href="{{ path('homepage') }}">Cancel</a></button>
        </div>
        <hr class="thin"/>
        <label for="user_login">Powered by Cypfrox</label>
    </form>
  </div>
</body>
</html>





   
